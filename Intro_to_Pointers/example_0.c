#include <stdio.h>

/**
 * main - program declares and itializes
 * a variable
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int num = 42;

	printf("The value of num is: %d\n", num);
	return (0);
}
